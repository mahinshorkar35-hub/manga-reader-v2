[package]
name = "manga-reader-plugin"
version = "0.1.0"
edition = "2021"
description = "Shared plugin trait definitions for Manga Reader v2 WASM plugins"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lib]
name = "manga_reader_plugin"
path = "plugin.rs"
